{% extends "common/base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    {% if product %}
        <h1>{{ product.name }}</h1>
        <div class="product-section">
            <div class="product-image">
                {% if product.image %}
                    <div class="image__wrap">
                        <img class="full-image" src="assets/img/{{ product.thumb }}" alt="{{ product.name }}">
                    </div>
                {% else %}
                    <p>Нет изображения в базе данных</p>
                {% endif %}
            </div>
            <div class="product-description">
                <div class="description-item">Наименование:
                    {{ product.name }}
                </div>
                <div class="description-item">Количество:
                    {{ product.quantity }}
                </div>
                <div class="description-item">Цена:
                    {{ product.price }}
                </div>
                <div class="description-item">Число просмотров:
                    {{ product.views }}
                </div>
                <div class="product-cart">
                    <a class="btn addToCart" data-id="{{ product.product_id }}"
                       href="product.php?addToCart=1&product_id={{ product.product_id }}"
                       title="Добавить товар в корзину">Добавить в корзину</a>
                </div>
            </div>
        </div>
    {% else %}
        <p>Нет товара в базе данных</p>
    {% endif %}

{% endblock %}